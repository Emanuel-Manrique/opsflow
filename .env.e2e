# Loaded by Nx for every `e2e` target, so the test runners look for the services on
# the ports .env.serve-e2e actually started them on rather than the development ones.
API_PORT=3200
ORCHESTRATOR_PORT=3201
WORKER_PORT=3202
BASE_URL=http://localhost:4300
